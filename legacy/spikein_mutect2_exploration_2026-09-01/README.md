# Spike-in Mutect2 exploration archive

This directory preserves the exploratory Mutect2 and FilterMutectCalls runs
completed on 1 September 2026. Paths below this directory mirror their former
locations in the repository.

The final selected settings are implemented in stages 8 and 9 under
`src2/spikein`. `MANIFEST.tsv` records the size and SHA-256 hash of every
archived file.
